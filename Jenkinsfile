pipeline {
    agent {label 'uitest'}
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello world!"..'
                echo "creating multibranch projects"
            }
        }
    }
}
