pipeline {
    agent {label 'chrome'}
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello world!"..'
                echo "creating multibranch projects"
            }
        }
    }
}
